database {
  postgres {
    connectionPool = "HikariCP"
    dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
    numThreads = 10
    properties = {
      serverName = "localhost"
      portNumber = "5433"
      databaseName = "bank"
      user = "postgres"
      password = "1234"
    }
  }
}